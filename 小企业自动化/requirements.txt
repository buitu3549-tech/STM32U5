# 小企业自动化工具箱 - 依赖清单
# 安装：pip install -r requirements.txt

# 核心数据处理
pandas>=2.0
openpyxl>=3.1
python-docx>=0.8

# 浏览器自动化（数据抓取）
playwright>=1.40

# Web 后台
streamlit>=1.28

# PDF 报表（可选）
weasyprint>=60

# HTTP 请求（消息推送、API 对接）
requests>=2.28

# 定时调度（方案一：Python 内置调度）
schedule>=1.2

# 数据库（进阶使用，可选）
# sqlalchemy>=2.0
